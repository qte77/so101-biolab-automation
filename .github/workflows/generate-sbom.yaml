name: Generate SBOM

on:
  push:
    branches: [main]
    paths-ignore:
      - "docs/SBOM/**"
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  generate-sbom:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Generate SBOM
        uses: qte77/gha-sbom-action@4006f051f0821516f9eb1143679ff35588ac1b84 # v0.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
