name: Write repo llms.txt

on:
  push:
    branches: [main]
    paths:
      - ".github/templates/llms.txt.tpl"
      - "app/**"
      - "README.md"
      - "CONTRIBUTING.md"
      - "AGENTS.md"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  generate-file:
    runs-on: ubuntu-latest
    env:
      REPO: ${{ github.repository }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3  # v6.0.0

      - name: Validate template links exist
        run: |
          stale=0
          for path in $(grep -oE '\$\{BLOB\}/[^)]+' .github/templates/llms.txt.tpl | sed 's|\${BLOB}/||'); do
            if [ ! -f "$path" ]; then
              echo "::error::Stale link in template: $path"
              stale=1
            fi
          done
          exit $stale

      - name: Generate llms.txt from template
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BLOB="https://github.com/${REPO}/blob/main"
          PROJECT_NAME=$(sed -n 's/^# //p' README.md | head -1)
          PROJECT_DESC=$(curl -sf -H "Authorization: token ${GH_TOKEN}" \
            "https://api.github.com/repos/${REPO}" | jq -r '.description // empty')
          export BLOB PROJECT_NAME PROJECT_DESC
          mkdir -p docs
          envsubst '${BLOB} ${PROJECT_NAME} ${PROJECT_DESC}' < .github/templates/llms.txt.tpl > docs/llms.txt

      - name: Create PR if changed
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add docs/llms.txt
          git diff --cached --quiet && echo "No changes" && exit 0
          BRANCH="auto/llms-txt-${{ github.run_number }}"
          git checkout -b "$BRANCH"
          git commit -m "docs: update llms.txt index"
          git push -u origin "$BRANCH"
          gh pr create --base main --head "$BRANCH" --title "docs: update llms.txt index" --body "Auto-generated by GHA"
